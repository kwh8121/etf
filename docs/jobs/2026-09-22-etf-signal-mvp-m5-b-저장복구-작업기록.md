# ETF 신호 MVP — M5-B 미국 P1 저장 복구 작업 기록

> 작업일: 2026-09-22 KST
> 상태: US 코드 보완 완료 · 실제 실패 복구 실측 대기
> 이전 핸드오프: `docs/jobs/2026-09-22-etf-signal-mvp-codex-핸드오프-작업기록.md`

## 결과

- 09:31 KST 기준 KR timer의 다음 실행은 9/22 19:15, US timer는 9/23 07:30으로 확인했다. GitHub Actions에는 그 이후 신규 실행이 없었다.
- 미국 P1 저장 경로에서 `signal_run`을 `PENDING`으로 먼저 기록하고 신호 행을 저장한 뒤에만 `COMPLETED`로 전환한다.
- 동일 콘텐츠의 기존 스냅샷에 연결된 실행이 미완료이면 새 스냅샷·원본 객체를 만들지 않고 기존 실행을 재사용해 신호 저장을 다시 시도한다. 완료된 실행의 동일 콘텐츠만 중복 관측으로 기록한다.
- 실행 조회는 시간 문자열의 직렬화 차이에 영향받지 않도록 원본 스냅샷 ID로 한다.

## 검증

- 대상 테스트: 저장 실패 시 `PENDING` 유지와 같은 콘텐츠 재시도 복구를 확인했다.
- 전체: Vitest 28 파일·87 테스트, `npx tsc --noEmit`, `npm run lint`, `npm run build` 통과.
- Supabase 읽기 전용 상태 조회: 기존 US P1 `signal_run`은 `COMPLETED` 3건이며 미완료 실행은 0건이었다. 새 복구 경로의 운영 쓰기 실측은 수행하지 않았다.
- 빌드 도중 이전 검사의 생성 잠금 파일 때문에 재시도가 한 번 필요했다. 이후 단독 빌드가 정상 종료됐다.
- Kiwoom·Supabase 운영 쓰기·Telegram 전송은 이 작업에서 수행하지 않았다.

## 남은 작업

1. 9/22 19:15 이후 KR timer 첫 자동 실행과 9/23 07:30 이후 US timer의 플래그 off 결과를 확인한다.
2. KR 신호 저장의 완료 상태 선기록 문제를 별도 테스트와 코드 변경으로 보완한다.
3. US 실제 실패 복구·미국 휴장 시간 중복 관측 실측은 운영 DB·staging 알림 부작용을 검토한 뒤 실행한다.
4. P1 보고 분량 정책과 runner 권한·I/O 위험을 결정·추적한다.

애플리케이션 단계의 완료 경계는 DB 트랜잭션과 다르다. 중간 `PENDING` 실행과 부분 신호 행은 실패 뒤 남을 수 있으며, 재시도는 같은 원본의 미완료 실행을 이어서 저장한다.
