# ETF 신호 MVP — 2026-09-22 일일 작업 종합

> 기준: 2026-09-22 15:41 KST, `main`/`origin/main`의 `8ecefa0`
> 성격: 당일 기록의 요약. 세부 근거는 아래 개별 작업 기록과 Git 커밋을 따른다.

## 오늘 완료한 작업

1. **운영 인계·상태 정합화**: self-hosted runner와 systemd timer 운영 인계 기록을 반영하고, Linear의 M5-B 구현 완료와 미검증 게이트를 분리했다. M6 관측은 자동 KR 실행 증거가 없어 0/20으로 유지했다. (`ac25493`~`6eef9bf`)
2. **저장 실패 복구 보완**: US P1은 신호 저장 전 `PENDING`, 모든 행 저장 후 `COMPLETED`로 전환하고 동일 콘텐츠의 미완료 실행을 재사용하도록 고쳤다. KR도 신호 저장 실패 시 완료된 빈 실행이 남지 않도록 `PENDING`→`COMPLETED` 경계를 보완했다. 실제 공급자 실패·재시도 운영 E2E는 아직 미검증이다. (`dc93c53`, `0ab8e10`)
3. **US 보고 분량 확정**: 유형별 상위 10건, 최대 40행으로 제한하고 전체 건수를 함께 표시한다. 운영 DB의 기존 완료 실행을 읽기 전용으로 포맷한 결과 메시지 1건이었다. 새 형식의 staging 실발송은 하지 않았다. (`ca589a6`, `3058942`)
4. **근무시간 제약 반영**: 회사 PC는 평일 09:00~18:00 KST에만 가동할 수 있음을 문서에 반영했다. 기존 설치 timer가 KR 19:15·US 07:30이어서 정시 실행을 보장하지 못한다는 점을 확인했다. (`1e9b3e3`)
5. **KR 누락일 보충 구현**: 사용자 결정에 따라 US까지의 무손실 필수 원칙은 철회하고 KR을 우선했다. KR timer **원본**을 평일 09:30으로 옮기고, 입력 없는 KR workflow가 2026-09-22부터 전일까지 평일을 순차 점검하도록 했다. KRX 일별 데이터가 누락되면 수집하고 기준일별 신호를 생성한다. 앞선 거래일이 뒤늦게 채워지면 뒤 날짜의 5·20거래일 신호를 재계산하며, 실패한 날짜에서 중단해 다음 실행에 재시도한다. (`8ecefa0`)

## 검증과 외부 상태

- KR 보충 최종 로컬 검증: `npm test` 29개 파일·90개 테스트, `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm run continuity:check`, `git diff --check` 통과. 커밋 `8ecefa0`까지 `origin/main`에 푸시했다.
- Linear KOR-58에는 KR 보충 범위·검증 결과와 **운영 설치/자동 E2E 대기, M6 0/20**을 기록했다. OpenViking에는 US 무손실 필수 철회와 KR 보충 경계를 재개 메모리로 저장했다.
- 오늘의 로컬 점검과 기존 완료 실행의 읽기 전용 조회는 운영 Supabase 쓰기, Kiwoom 재수집, Telegram 발송을 수행하지 않았다. 따라서 새 KR 보충 경로의 운영 성공을 입증하지 않는다.
- 운영 PC의 `~/.config/systemd/user/` timer와 `~/.local/lib/etf-ops/` dispatch 스크립트 **설치본은 변경하지 않았다**. 저장소 원본만 09:30 방식이므로 실제 자동 실행은 여전히 미검증이다.
- 작업 사본의 기존 미추적 `supabase/.temp/`는 사용자 로컬 파일로 보존했다. 이 종합 기록을 작성할 때 발견한 다른 미커밋 문서 변경도 수정하지 않았다.

## 남은 작업과 순서

1. KR 설치본 갱신 시 `Persistent=true`가 즉시 운영 실행을 유발할 수 있으므로 발화 시각·영향을 확인한 뒤 적용한다. 설치 후 timer journal → GitHub run → KRX·신호 저장 결과를 대조한다. 보충은 Telegram을 보내지 않으며 과거 Kiwoom 마스터도 재현하지 않으므로 과거 날짜를 M6 완전 관측일로 자동 산입하지 않는다.
2. KR 첫 자동 경로의 운영 E2E와 저장 실패·재시도 실측을 완료하고 KOR-58·KOR-59를 증거에 맞게 갱신한다. 그 전에는 M6 관측을 0/20으로 유지한다.
3. US timer의 근무시간대 재편, 미국 휴장 시간 동일 콘텐츠 중복 관측, 실제 실패 복구, 새 상위 10건 보고의 staging 실발송을 별도로 검증한다. US 무손실은 필수 게이트가 아니다.
4. KRX 과거 평일의 빈 응답은 휴장과 일시적 공급자 장애를 자동으로 구분할 수 없다. `NON_TRADING` 평일 재조회 결과를 운영 관측에서 확인한다. 신호 행 교체는 DB 단일 트랜잭션이 아니므로 중간 실패 후 일시적 부분 표시 가능성을 추적한다.

## 상세 기록

- `docs/jobs/2026-09-22-etf-signal-mvp-codex-핸드오프-작업기록.md`
- `docs/jobs/2026-09-22-etf-signal-mvp-linear-상태정합-작업기록.md`
- `docs/jobs/2026-09-22-etf-signal-mvp-m5-b-저장복구-작업기록.md`
- `docs/jobs/2026-09-22-etf-signal-mvp-kr-저장복구-작업기록.md`
- `docs/jobs/2026-09-22-etf-signal-mvp-us-보고분량-작업기록.md`
- `docs/jobs/2026-09-22-etf-signal-mvp-운영시간-정합-작업기록.md`
- `docs/jobs/2026-09-22-etf-signal-mvp-kr-누락일-보충-작업기록.md`
